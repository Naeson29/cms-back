import React from 'react';
// import PropTypes from 'prop-types';

/**
 *
 * @returns {JSX.Element}
 * @constructor
 */
const Form = () => (
    <form className="form">coucou form</form>
);

Form.propTypes = {

};

Form.defaultProps = {

};

export default Form;
