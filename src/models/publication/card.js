
import React from 'react';
import { getImage } from '../../utilities/functions';

export default (key) => {
    return (
        <div className="card-publication">
            
        </div>
    );
};
